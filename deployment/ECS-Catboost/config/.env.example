# Deployment environment: "local" or "cloud"
DEPLOYMENT_ENV=cloud

# API URL for predictions (local or cloud deployment)
API_URL=http://your-api-url-here

# Paths for test data (used in local mode)
X_TEST_PATH=X_test.csv
Y_TEST_PATH=y_test.csv

# Cloud storage settings (used when DEPLOYMENT_ENV=cloud)
S3_BUCKET_NAME="your-s3-bucket-name"
MODEL_FILE_NAME="your-model-file.keras"

